# L1 引擎能力 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为千世书引擎加一组双模式共享的原语——印记(flags)、条件 `has()`、机缘封顶、加权分支 outcomes、强制结局、身份印记、可选 maxTurns + 涌现终止——支撑后续 xian 机缘重构。

**Architecture:** 纯函数引擎（`src/engine/`）+ Zod 剧本 schema（`src/scenarios/schema.ts`）。新字段全部可选、向后兼容旧存档/旧剧本。本地与 AI 两模式都把回合表达成 `TurnResult`，引擎统一消费；本期只让本地产出新字段，AI 产出留待 L2。

**Tech Stack:** TypeScript、React 18、Vite、Vitest、Zod。

## Global Constraints

- 所有新 schema/类型字段**必须可选**；旧存档无 `flags` 视为 `[]`、无 `ceiling` 视为 `max`、无 `outcomes` 走 `effects`、无 `maxTurns` 走涌现终止。
- 测试框架 Vitest，`test: { environment: 'node' }`；运行单测用 `npx vitest run <file> -t "<name>"`。
- 每步骤 commit 信息以 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` 与 `Claude-Session: https://claude.ai/code/session_01PHzsPTk6RFo3GtatFSduqL` 两行结尾。
- 提交前确保 `npx tsc --noEmit` 与 `npx vitest run` 全绿。
- 中文注释/文案风格与现有文件一致。

---

## File Structure

- `src/engine/types.ts` — `GameState` 加 `flags`；`Choice` 加 `outcomes/flagsSet/flagsClear/endTone`；新增 `Outcome`。
- `src/scenarios/schema.ts` — attribute 加 `ceiling/ceilingUnlocks`；opening 加 `flag`；localChoice 加 `outcomes/flagsSet/flagsClear/endTone`；新增 `outcomeSchema`；localEvent 加 `wildcard`；`maxTurns` 改可选。
- `src/engine/condition.ts` — Clause 加 `has`；`evalCondition` 加 `flags` 参数、`maxTurns` 可空。
- `src/engine/state.ts` — `initState` 写 flags；`effectiveCeiling`/`clampEffects` 用 flags；`rollOutcome`/`applyFlags`；`applyChoice`/`resolveCustomAction` 整合；`checkEnding` 加 flags、maxTurns 可空、硬兜底。
- `src/engine/local.ts` — `pickLocalEvent` 传 flags + wildcard 保底 + progress 兜底；`localTurn` 透传新字段。
- `src/engine/grade.ts` — `surv` 处理 maxTurns 可空。
- `src/ui/Setup.tsx` — 删「人生长度」。

---

## Task 1: flags 状态 + 开局身份印记

**Files:**
- Modify: `src/engine/types.ts`（`GameState`）
- Modify: `src/scenarios/schema.ts`（`openingSchema`）
- Modify: `src/engine/state.ts`（`initState`）
- Test: `src/engine/state.test.ts`

**Interfaces:**
- Produces: `GameState.flags?: string[]`；`Opening.flag?: string`；`initState` 写入 `flags`。

- [ ] **Step 1: 写失败测试**

在 `src/engine/state.test.ts` 的 `describe('initState', ...)` 内加：

```ts
it('按 opening.flag 写入身份印记，无 flag 则为空', () => {
  const withFlag = scenarioSchema.parse({
    id: 'f', title: 'F', emoji: '🚩', intro: '开局',
    attributes: [{ key: 'hp', name: '生命', initial: 80, max: 100, deathBelow: 0 }],
    openings: [{ name: '魔道', prompt: '魔功传人', flag: '魔道' }],
    maxTurns: 5, systemPrompt: 'GM', endings: [{ condition: 'maxTurns', tone: '终' }],
  })
  expect(initState(withFlag, withFlag.openings![0]).flags).toEqual(['魔道'])
  expect(initState(withFlag).flags).toEqual([])
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/state.test.ts -t "身份印记"`
Expected: FAIL（`flag` 不被 schema 接受 / `flags` undefined）

- [ ] **Step 3: 实现**

`src/engine/types.ts` 的 `GameState` 加字段（放在 `ended?` 上一行）：

```ts
  // 印记：隐藏的具名状态（身份/机缘/因果种子），门控事件、结局、属性封顶
  flags?: string[]
```

`src/scenarios/schema.ts` 的 `openingSchema`：

```ts
export const openingSchema = z.object({
  name: z.string().min(1),
  prompt: z.string().min(1),
  // 开局即写入的身份印记（如 '魔道'），用于身份门控事件
  flag: z.string().optional(),
})
```

`src/engine/state.ts` 的 `initState` 的 `return` 对象加一行（在 `memory: [],` 后）：

```ts
    flags: opening?.flag ? [opening.flag] : [],
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/state.test.ts -t "身份印记"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/types.ts src/scenarios/schema.ts src/engine/state.ts src/engine/state.test.ts
git commit -m "feat(engine): flags 印记状态 + 开局身份印记"
```

---

## Task 2: 条件语言 has()/!has() + flags 参数

**Files:**
- Modify: `src/engine/condition.ts`
- Test: `src/engine/condition.test.ts`

**Interfaces:**
- Consumes: 无。
- Produces: `Clause` 增 `{ kind: 'has'; flag: string; neg: boolean }`；`evalCondition(c, attrs, completedTurns, maxTurns: number | undefined, flags?: string[])`；`evalClause` 同步。`maxTurns` 现可为 `undefined`（涌现剧本）。

- [ ] **Step 1: 写失败测试**

在 `src/engine/condition.test.ts` 末尾加：

```ts
import { parseCondition, evalCondition } from './condition'

describe('has() 印记条件', () => {
  it('解析并判定 has / !has', () => {
    expect(evalCondition(parseCondition('has(金丹)'), {}, 0, 30, ['金丹'])).toBe(true)
    expect(evalCondition(parseCondition('has(金丹)'), {}, 0, 30, [])).toBe(false)
    expect(evalCondition(parseCondition('!has(结怨)'), {}, 0, 30, [])).toBe(true)
    expect(evalCondition(parseCondition('!has(结怨)'), {}, 0, 30, ['结怨'])).toBe(false)
  })
  it('与属性子句用 & 组合', () => {
    const c = parseCondition('hp>=10 & has(金丹)')
    expect(evalCondition(c, { hp: 20 }, 0, 30, ['金丹'])).toBe(true)
    expect(evalCondition(c, { hp: 20 }, 0, 30, [])).toBe(false)
  })
  it('maxTurns 为 undefined 时 maxTurns 子句恒 false（涌现剧本）', () => {
    expect(evalCondition(parseCondition('maxTurns'), {}, 99, undefined, [])).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/condition.test.ts -t "印记条件"`
Expected: FAIL（`has(...)` 解析抛错）

- [ ] **Step 3: 实现**

整体替换 `src/engine/condition.ts` 为：

```ts
export type Clause =
  | { kind: 'maxTurns' }
  | { kind: 'has'; flag: string; neg: boolean }
  | { kind: 'cmp'; attr: string; op: '<=' | '>='; value: number }

// 条件 = 一个或多个子句的「与」（用 & 连接）。单子句时 parts 仅一项。
export type Condition = Clause | { kind: 'and'; parts: Clause[] }

function parseClause(input: string): Clause {
  const s = input.trim()
  if (s === 'maxTurns') return { kind: 'maxTurns' }
  const h = s.match(/^(!?)has\(\s*([^)]+?)\s*\)$/)
  if (h) return { kind: 'has', flag: h[2], neg: h[1] === '!' }
  const m = s.match(/^([a-z][a-zA-Z0-9_]*)\s*(<=|>=)\s*(-?\d+)$/)
  if (!m) throw new Error(`无法解析结局条件: ${input}`)
  return { kind: 'cmp', attr: m[1], op: m[2] as '<=' | '>=', value: Number(m[3]) }
}

export function parseCondition(input: string): Condition {
  const parts = input.split('&').map((p) => p.trim()).filter(Boolean)
  if (parts.length === 0) throw new Error(`无法解析结局条件: ${input}`)
  if (parts.length === 1) return parseClause(parts[0])
  return { kind: 'and', parts: parts.map(parseClause) }
}

function evalClause(
  c: Clause,
  attrs: Record<string, number>,
  completedTurns: number,
  maxTurns: number | undefined,
  flags: string[],
): boolean {
  if (c.kind === 'maxTurns') return maxTurns !== undefined && completedTurns >= maxTurns
  if (c.kind === 'has') {
    const present = flags.includes(c.flag)
    return c.neg ? !present : present
  }
  const v = attrs[c.attr]
  if (v === undefined) return false
  return c.op === '<=' ? v <= c.value : v >= c.value
}

export function evalCondition(
  c: Condition,
  attrs: Record<string, number>,
  completedTurns: number,
  maxTurns: number | undefined,
  flags: string[] = [],
): boolean {
  if (c.kind === 'and')
    return c.parts.every((p) => evalClause(p, attrs, completedTurns, maxTurns, flags))
  return evalClause(c, attrs, completedTurns, maxTurns, flags)
}

// 提取条件中引用的所有属性 key（供 import 校验属性是否存在）
export function conditionAttrs(c: Condition): string[] {
  const clauses = c.kind === 'and' ? c.parts : [c]
  return clauses.flatMap((p) => (p.kind === 'cmp' ? [p.attr] : []))
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/condition.test.ts`
Expected: PASS（含原有用例，`flags` 默认 `[]` 向后兼容）

- [ ] **Step 5: Commit**

```bash
git add src/engine/condition.ts src/engine/condition.test.ts
git commit -m "feat(engine): 条件语言支持 has()/!has() 印记 + maxTurns 可空"
```

---

## Task 3: 机缘封顶 ceiling + ceilingUnlocks

**Files:**
- Modify: `src/scenarios/schema.ts`（`attributeSchema`）
- Modify: `src/engine/state.ts`（`effectiveCeiling`、`clampEffects`）
- Test: `src/engine/state.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `flags`。
- Produces: `Attribute` 增 `ceiling?: number`、`ceilingUnlocks?: { flag: string; max: number }[]`；`effectiveCeiling(a, flags): number`；`clampEffects(sc, attrs, effects, flags?: string[])`（新增第 4 参 `flags`，默认 `[]`）。

- [ ] **Step 1: 写失败测试**

在 `src/engine/state.test.ts` 加：

```ts
describe('机缘封顶 ceiling/ceilingUnlocks', () => {
  const capSc: Scenario = scenarioSchema.parse({
    id: 'cap', title: 'C', emoji: '⛰️', intro: '开局',
    attributes: [{
      key: 'cultivation', name: '修为', initial: 10, max: 100,
      ceiling: 45, ceilingUnlocks: [{ flag: '金丹', max: 70 }],
    }],
    maxTurns: 5, systemPrompt: 'GM', endings: [{ condition: 'maxTurns', tone: '终' }],
  })
  it('无印记时截在 ceiling', () => {
    expect(clampEffects(capSc, { cultivation: 44 }, { cultivation: 10 }, []).cultivation).toBe(45)
  })
  it('持有解锁印记后截在更高上限', () => {
    expect(clampEffects(capSc, { cultivation: 44 }, { cultivation: 50 }, ['金丹']).cultivation).toBe(70)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/state.test.ts -t "机缘封顶"`
Expected: FAIL（`ceiling` 不被接受 / 截在 100）

- [ ] **Step 3: 实现**

`src/scenarios/schema.ts` 的 `attributeSchema`，在 `decayPerTurn` 行后加：

```ts
    // 有效上限：初始 ceiling（缺省=max），持有 ceilingUnlocks 中的印记后逐级抬高。
    // 用于「机缘封顶」——无突破印记则修为/寿元卡在低位。
    ceiling: z.number().optional(),
    ceilingUnlocks: z.array(z.object({ flag: z.string(), max: z.number() })).optional(),
```

`src/engine/state.ts`，在 `clampEffects` 上方加 `effectiveCeiling`，并改 `clampEffects` 签名/上限：

```ts
import type { Scenario, Opening, Attribute } from '../scenarios/schema'
```
（若已有 `Scenario, Opening` 导入，补上 `Attribute`。）

```ts
// 属性的有效上限：基线 ceiling（缺省 max），叠加已持有印记解锁的更高上限，最终不超过 max。
export function effectiveCeiling(a: Attribute, flags: string[]): number {
  let cap = a.ceiling ?? a.max
  for (const u of a.ceilingUnlocks ?? []) if (flags.includes(u.flag)) cap = Math.max(cap, u.max)
  return Math.min(a.max, cap)
}
```

改 `clampEffects`：

```ts
export function clampEffects(
  sc: Scenario,
  attrs: Record<string, number>,
  effects: Record<string, number>,
  flags: string[] = [],
): Record<string, number> {
  const next = { ...attrs }
  for (const a of sc.attributes) {
    const d = effects[a.key]
    if (typeof d === 'number' && Number.isFinite(d)) {
      next[a.key] = Math.min(effectiveCeiling(a, flags), Math.max(0, next[a.key] + d))
    }
  }
  return next
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/state.test.ts -t "机缘封顶"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/scenarios/schema.ts src/engine/state.ts src/engine/state.test.ts
git commit -m "feat(engine): 机缘封顶 ceiling/ceilingUnlocks（印记解锁上限）"
```

---

## Task 4: 加权分支 outcomes（类型 + schema + rollOutcome）

**Files:**
- Modify: `src/engine/types.ts`（`Choice`、新增 `Outcome`）
- Modify: `src/scenarios/schema.ts`（`outcomeSchema`、`localChoiceSchema`）
- Modify: `src/engine/state.ts`（`rollOutcome`）
- Test: `src/engine/state.test.ts`

**Interfaces:**
- Produces: `Outcome` 接口；`Choice` 增 `outcomes?: Outcome[]`、`flagsSet?: string[]`、`flagsClear?: string[]`、`endTone?: string`；`rollOutcome(choice, rng): Outcome | null`（无 outcomes 返回 `null`，否则按 `weight` 加权取一）。

- [ ] **Step 1: 写失败测试**

```ts
import { rollOutcome } from './state'

describe('rollOutcome 加权分支', () => {
  const choice = {
    text: '帮老人', outcomes: [
      { weight: 1, effects: { hp: -5 }, flagsSet: ['结怨'] },
      { weight: 3, effects: { hp: 5 } },
    ],
  }
  it('无 outcomes 返回 null', () => {
    expect(rollOutcome({ text: 'x', effects: {} }, () => 0)).toBeNull()
  })
  it('rng 落在首段权重内取第一项', () => {
    // total=4，rng()*4=0 → 命中 weight1 的第一项
    expect(rollOutcome(choice, () => 0)?.flagsSet).toEqual(['结怨'])
  })
  it('rng 落在后段取第二项', () => {
    // rng()=0.9 → 0.9*4=3.6 > 1 → 第二项
    expect(rollOutcome(choice, () => 0.9)?.effects).toEqual({ hp: 5 })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/state.test.ts -t "加权分支"`
Expected: FAIL（`rollOutcome` 未定义）

- [ ] **Step 3: 实现**

`src/engine/types.ts`，在 `Choice` 之上加 `Outcome`，并扩展 `Choice`：

```ts
export interface Outcome {
  weight?: number
  effects?: Record<string, number>
  reaction?: string
  narrative?: string
  flagsSet?: string[]
  flagsClear?: string[]
  itemsGained?: string[]
  itemsLost?: string[]
  // 命中即强制结局，无视数值
  endTone?: string
}

export interface Choice {
  text: string
  effects: Record<string, number>
  reaction?: string
  // 加权分支：存在则引擎掷骰取一（跳过命运无常），否则走 effects
  outcomes?: Outcome[]
  flagsSet?: string[]
  flagsClear?: string[]
  endTone?: string
}
```

`src/scenarios/schema.ts`，在 `localChoiceSchema` 之上加 `outcomeSchema`，并扩展 `localChoiceSchema`：

```ts
export const outcomeSchema = z.object({
  weight: z.number().positive().default(1),
  effects: z.record(z.string(), z.number()).default({}),
  reaction: z.string().optional(),
  narrative: z.string().optional(),
  flagsSet: z.array(z.string()).optional(),
  flagsClear: z.array(z.string()).optional(),
  itemsGained: z.array(z.string().min(1)).optional(),
  itemsLost: z.array(z.string().min(1)).optional(),
  endTone: z.string().optional(),
})

export const localChoiceSchema = z.object({
  text: z.string().min(1),
  effects: z.record(z.string(), z.number()).default({}),
  reaction: z.string().optional(),
  // 加权分支：存在则引擎掷骰取一，每分支自带 effects/印记/可选强制结局
  outcomes: z.array(outcomeSchema).min(1).optional(),
  flagsSet: z.array(z.string()).optional(),
  flagsClear: z.array(z.string()).optional(),
  endTone: z.string().optional(),
})
```

`src/engine/state.ts`，加 `rollOutcome`（放在 `rollFortune` 附近）：

```ts
import type { Choice, GameState, TurnResult, Ending, Outcome } from './types'
```
（补 `Outcome` 到现有 types 导入。）

```ts
// 加权分支：无 outcomes 返回 null；否则按 weight 加权掷骰取一
export function rollOutcome(choice: Choice, rng: () => number): Outcome | null {
  const outs = choice.outcomes
  if (!outs || outs.length === 0) return null
  const total = outs.reduce((s, o) => s + (o.weight ?? 1), 0)
  let r = rng() * total
  for (const o of outs) {
    r -= o.weight ?? 1
    if (r <= 0) return o
  }
  return outs[outs.length - 1]
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/state.test.ts -t "加权分支"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/types.ts src/scenarios/schema.ts src/engine/state.ts src/engine/state.test.ts
git commit -m "feat(engine): 加权分支 outcomes 类型/schema/rollOutcome"
```

---

## Task 5: applyChoice 整合 outcomes + flags + 封顶

**Files:**
- Modify: `src/engine/state.ts`（`applyChoice`、新增 `applyFlags`）
- Test: `src/engine/state.test.ts`

**Interfaces:**
- Consumes: Task 3 `clampEffects(...,flags)`、Task 4 `rollOutcome`。
- Produces: `applyFlags(flags, set?, clear?): string[]`；`applyChoice` 现处理 outcomes（跳过命运无常）、写 flags、按印记封顶。

- [ ] **Step 1: 写失败测试**

```ts
describe('applyChoice 整合 outcomes/flags', () => {
  const sc2: Scenario = scenarioSchema.parse({
    id: 'i', title: 'I', emoji: '🎯', intro: '开局',
    attributes: [{ key: 'hp', name: '生命', initial: 50, max: 100, deathBelow: 0 }],
    maxTurns: 5, systemPrompt: 'GM', endings: [{ condition: 'maxTurns', tone: '终' }],
  })
  const turnOut = (): TurnResult => ({
    narrative: '剧情', summary: '摘要',
    choices: [{ text: '赌', effects: {}, outcomes: [
      { weight: 1, effects: { hp: 10 }, flagsSet: ['吉'] },
    ] }],
  })
  it('outcomes 分支应用 effects 与 flags，且跳过命运无常缩放', () => {
    const st = applyChoice(sc2, initState(sc2, undefined, undefined, 'local'), turnOut(), 0, () => 0)
    expect(st.attributes.hp).toBe(60)        // 10 原值，未被放大
    expect(st.flags).toEqual(['吉'])
  })
  it('flagsClear 移除印记', () => {
    let st = initState(sc2, undefined, undefined, 'local')
    st = { ...st, flags: ['吉', '凶'] }
    const tr: TurnResult = { narrative: 'n', summary: 's',
      choices: [{ text: 'c', effects: {}, flagsClear: ['凶'] }] }
    expect(applyChoice(sc2, st, tr, 0).flags).toEqual(['吉'])
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/state.test.ts -t "整合 outcomes"`
Expected: FAIL（flags 未变 / hp 被命运无常放大）

- [ ] **Step 3: 实现**

`src/engine/state.ts` 加 `applyFlags`（放在 `applyItems` 附近）：

```ts
// 应用印记增减：先去掉 clear，再并入 set（去重）
export function applyFlags(flags: string[], set?: string[], clear?: string[]): string[] {
  const cleared = new Set((clear ?? []).map((s) => s.trim()).filter(Boolean))
  const kept = flags.filter((f) => !cleared.has(f))
  for (const raw of set ?? []) {
    const f = raw.trim()
    if (f && !kept.includes(f)) kept.push(f)
  }
  return kept
}
```

替换整个 `applyChoice` 为：

```ts
export function applyChoice(
  sc: Scenario,
  st: GameState,
  turnRes: TurnResult,
  choiceIdx: number,
  rng?: () => number,
): GameState {
  const choice = turnRes.choices[choiceIdx]
  if (!choice) throw new Error(`选项不存在: ${choiceIdx}`)
  const flags0 = st.flags ?? []

  // 有 outcomes 则掷骰取一分支（跳过命运无常）；否则走旧 effects + 命运无常
  const picked = rng ? rollOutcome(choice, rng) : (choice.outcomes?.[0] ?? null)
  let baseEffects: Record<string, number>
  let twist: string | undefined
  let setFlags = choice.flagsSet
  let clearFlags = choice.flagsClear
  let endTone = choice.endTone
  let reaction = choice.reaction
  let itemsGained = turnRes.itemsGained
  let itemsLost = turnRes.itemsLost
  if (picked) {
    baseEffects = picked.effects ?? {}
    setFlags = picked.flagsSet ?? setFlags
    clearFlags = picked.flagsClear ?? clearFlags
    endTone = picked.endTone ?? endTone
    reaction = picked.reaction ?? reaction
    if (picked.itemsGained) itemsGained = picked.itemsGained
    if (picked.itemsLost) itemsLost = picked.itemsLost
  } else if (rng) {
    const f = rollFortune(choice.effects, rng)
    baseEffects = f.effects
    twist = f.twist
  } else {
    baseEffects = choice.effects
  }

  const attributes = clampEffects(sc, st.attributes, mergeEffects(baseEffects, decayEffects(sc)), flags0)
  const flags = applyFlags(flags0, setFlags, clearFlags)
  const history = [
    ...st.history,
    {
      narrative: turnRes.narrative,
      choiceText: choice.text,
      summary: turnRes.summary,
      ...(reaction ? { reaction } : {}),
      ...(twist ? { twist } : {}),
    },
  ]
  const ended = endTone
    ? { tone: endTone, reason: 'forced' }
    : checkEnding(sc, attributes, history.length, flags) ?? undefined
  return {
    ...st,
    attributes,
    flags,
    history,
    inventory: applyItems(st.inventory ?? [], { ...turnRes, itemsGained, itemsLost }),
    memory: applyMemory(st.memory, turnRes.memoryAdd),
    goalProgress: nextProgress(st.goalProgress, turnRes.goalProgress),
    ended,
  }
}
```

**保持 tsc 绿的关键**：本步同时给 `checkEnding` 加上可选第 4 参 `flags`（仅签名 + 透传，**不**改 maxTurns 可空/硬兜底逻辑——那些在 Task 7）。即把 `export function checkEnding(sc, attrs, completedTurns)` 改为 `export function checkEnding(sc, attrs, completedTurns, flags: string[] = [])`，并把其内部两处 `evalCondition(c, attrs, completedTurns, sc.maxTurns)` 补成 `evalCondition(c, attrs, completedTurns, sc.maxTurns, flags)`。这样 Task 5 结束时 tsc 仍为 0；Task 7 再整段替换 `checkEnding` 加入 maxTurns 可空与硬兜底。强制结局 `endTone` 分支由本任务的 applyChoice 提供，Task 6 补测。

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/state.test.ts -t "整合 outcomes"`
Expected: PASS（运行期通过；`checkEnding` 多传一个 `flags` 实参对当前实现无害，Task 7 正式接收）

- [ ] **Step 5: Commit**

```bash
git add src/engine/state.ts src/engine/state.test.ts
git commit -m "feat(engine): applyChoice 整合 outcomes/印记/封顶"
```

---

## Task 6: 强制结局 forceEnding（endTone）

**Files:**
- Modify: `src/engine/state.ts`（确认 `applyChoice` 的 `endTone` 分支）
- Test: `src/engine/state.test.ts`

**Interfaces:**
- Consumes: Task 5 `applyChoice` 的 `endTone` 逻辑。
- Produces: 选项/分支带 `endTone` 时 `state.ended = { tone: endTone, reason: 'forced' }`，无视当前属性。

- [ ] **Step 1: 写失败测试**

```ts
describe('forceEnding 强制结局（天堂地狱）', () => {
  const sc3: Scenario = scenarioSchema.parse({
    id: 'h', title: 'H', emoji: '🎲', intro: '开局',
    attributes: [{ key: 'hp', name: '生命', initial: 90, max: 100, deathBelow: 0 }],
    maxTurns: 30, systemPrompt: 'GM',
    endings: [{ condition: 'maxTurns', tone: '终' }, { condition: 'hp<=0', tone: '死' }],
  })
  it('endTone 无视数值直接定结局', () => {
    const tr: TurnResult = { narrative: 'n', summary: 's',
      choices: [{ text: '踩中凶煞', effects: {}, endTone: '当场暴毙' }] }
    const st = applyChoice(sc3, initState(sc3, undefined, undefined, 'local'), tr, 0)
    expect(st.ended).toEqual({ tone: '当场暴毙', reason: 'forced' })
    expect(st.attributes.hp).toBe(90) // 数值健康也照样结束
  })
  it('outcomes 分支也能携带 endTone', () => {
    const tr: TurnResult = { narrative: 'n', summary: 's',
      choices: [{ text: '探秘境', effects: {}, outcomes: [{ weight: 1, endTone: '得道飞升' }] }] }
    const st = applyChoice(sc3, initState(sc3, undefined, undefined, 'local'), tr, 0, () => 0)
    expect(st.ended?.tone).toBe('得道飞升')
  })
})
```

- [ ] **Step 2: 跑测试确认失败 / 通过**

Run: `npx vitest run src/engine/state.test.ts -t "强制结局"`
Expected: 若 Task 5 已含 `endTone` 分支则 PASS；若调整后未生效则按 Task 5 Step 3 的 `endTone` 逻辑修正至 PASS。

- [ ] **Step 3: Commit**

```bash
git add src/engine/state.ts src/engine/state.test.ts
git commit -m "feat(engine): forceEnding 强制结局（隐藏天堂地狱）"
```

---

## Task 7: maxTurns 可选 + 涌现终止 + checkEnding flags

**Files:**
- Modify: `src/scenarios/schema.ts`（`maxTurns` 可选）
- Modify: `src/engine/state.ts`（`checkEnding`、`resolveCustomAction`）
- Modify: `src/engine/grade.ts`（`surv` 处理可空 maxTurns）
- Test: `src/engine/state.test.ts`

**Interfaces:**
- Consumes: Task 2 `evalCondition(...,maxTurns?,flags)`。
- Produces: `checkEnding(sc, attrs, completedTurns, flags?: string[])`；`maxTurns` 可为 `undefined`（涌现：仅靠 deathBelow/结局/硬兜底终止）；硬兜底常量 `EMERGENT_HARD_CAP = 300`。

- [ ] **Step 1: 写失败测试**

```ts
describe('maxTurns 可选 + 涌现终止', () => {
  const emergent: Scenario = scenarioSchema.parse({
    id: 'e', title: 'E', emoji: '♾️', intro: '开局',
    attributes: [{ key: 'life', name: '寿元', initial: 60, max: 100, deathBelow: 0 }],
    systemPrompt: 'GM', // 注意：无 maxTurns
    endings: [{ condition: 'life<=0', tone: '油尽' }],
  })
  it('无 maxTurns 时不在第 30 回合自动落幕', () => {
    expect(checkEnding(emergent, { life: 50 }, 30)).toBeNull()
  })
  it('无 maxTurns 时寿元归零仍正常死亡', () => {
    expect(checkEnding(emergent, { life: 0 }, 10)?.tone).toBe('油尽')
  })
  it('无 maxTurns 时到达硬兜底强制收束', () => {
    expect(checkEnding(emergent, { life: 50 }, 300)?.reason).toBe('hardcap')
  })
  it('结局条件能读 flags', () => {
    const sc = scenarioSchema.parse({
      id: 's', title: 'S', emoji: '🚩', intro: '开局',
      attributes: [{ key: 'hp', name: '生命', initial: 50, max: 100, deathBelow: 0 }],
      maxTurns: 30, systemPrompt: 'GM',
      endings: [{ condition: 'maxTurns & has(金丹)', tone: '功成' }, { condition: 'maxTurns', tone: '蹉跎' }],
    })
    expect(checkEnding(sc, { hp: 50 }, 30, ['金丹'])?.tone).toBe('功成')
    expect(checkEnding(sc, { hp: 50 }, 30, [])?.tone).toBe('蹉跎')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/state.test.ts -t "可选"`
Expected: FAIL（无 maxTurns 解析报错 / 30 回合落幕 / 无 hardcap）

- [ ] **Step 3: 实现**

`src/scenarios/schema.ts`：`maxTurns: z.number().int().positive()` → `maxTurns: z.number().int().positive().optional()`。

`src/engine/state.ts`，替换 `checkEnding`：

```ts
// 涌现剧本（无 maxTurns）的硬兜底：到此回合数强制收束，防病态无限循环
export const EMERGENT_HARD_CAP = 300

export function checkEnding(
  sc: Scenario,
  attrs: Record<string, number>,
  completedTurns: number,
  flags: string[] = [],
): Ending | null {
  const dead = sc.attributes.filter(
    (a) => a.deathBelow !== undefined && attrs[a.key] <= a.deathBelow,
  )
  if (dead.length > 0) {
    const custom = sc.endings.find((e) => {
      const c = parseCondition(e.condition)
      return (
        c.kind === 'cmp' &&
        c.op === '<=' &&
        dead.some((a) => c.attr === a.key && c.value <= a.deathBelow!) &&
        evalCondition(c, attrs, completedTurns, sc.maxTurns, flags)
      )
    })
    if (custom) return { tone: custom.tone, reason: custom.condition }
    return { tone: '死亡', reason: `${dead[0].name}耗尽` }
  }
  for (const e of sc.endings) {
    if (evalCondition(parseCondition(e.condition), attrs, completedTurns, sc.maxTurns, flags)) {
      return { tone: e.tone, reason: e.condition }
    }
  }
  // 软上限剧本：到 maxTurns 兜底收束
  if (sc.maxTurns !== undefined && completedTurns >= sc.maxTurns) return { tone: '落幕', reason: 'maxTurns' }
  // 涌现剧本：高位硬兜底
  if (sc.maxTurns === undefined && completedTurns >= EMERGENT_HARD_CAP)
    return { tone: '落幕', reason: 'hardcap' }
  return null
}
```

同文件 `resolveCustomAction` 内，把：
```ts
  const attributes = clampEffects(
    sc,
    st.attributes,
    mergeEffects(resolved.actionEffects ?? {}, decayEffects(sc)),
  )
```
改为带 flags：
```ts
  const flags = st.flags ?? []
  const attributes = clampEffects(
    sc,
    st.attributes,
    mergeEffects(resolved.actionEffects ?? {}, decayEffects(sc)),
    flags,
  )
```
并把该函数 `return` 里的 `ended: checkEnding(sc, attributes, history.length) ?? undefined` 改为：
```ts
    ended: checkEnding(sc, attributes, history.length, flags) ?? undefined,
```
并在返回对象加 `flags,`（沿用，自定义行动不改印记）。

`src/engine/grade.ts`，把：
```ts
  const surv = sc.maxTurns > 0 ? Math.min(1, st.history.length / sc.maxTurns) : 0
```
改为（涌现剧本用一个名义长度做存活度代理）：
```ts
  const span = sc.maxTurns ?? 60
  const surv = span > 0 ? Math.min(1, st.history.length / span) : 0
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/state.test.ts && npx tsc --noEmit`
Expected: PASS；tsc 0（Task 5 的 `checkEnding(...,flags)` 调用此时签名匹配）

- [ ] **Step 5: Commit**

```bash
git add src/scenarios/schema.ts src/engine/state.ts src/engine/grade.ts src/engine/state.test.ts
git commit -m "feat(engine): maxTurns 可选 + 涌现终止/硬兜底 + checkEnding 读 flags"
```

---

## Task 8: pickLocalEvent 传 flags + has() 门控 + wildcard + progress 兜底

**Files:**
- Modify: `src/scenarios/schema.ts`（`localEventSchema` 加 `wildcard`）
- Modify: `src/engine/local.ts`（`pickLocalEvent`）
- Test: `src/engine/local.test.ts`

**Interfaces:**
- Consumes: Task 2 `evalCondition(...,flags)`。
- Produces: 事件 `requires` 可用 `has()`；`localEvent.wildcard?: boolean` 获保底权重；`progress` 在 `maxTurns` 缺省时用名义长度。

- [ ] **Step 1: 写失败测试**

在 `src/engine/local.test.ts` 加：

```ts
import { pickLocalEvent } from './local'
import { scenarioSchema } from '../scenarios/schema'
import { initState } from './state'

describe('pickLocalEvent 印记门控', () => {
  const sc = scenarioSchema.parse({
    id: 'g', title: 'G', emoji: '🎴', intro: '开局',
    attributes: [{ key: 'hp', name: '生命', initial: 50, max: 100, deathBelow: 0 }],
    maxTurns: 30, systemPrompt: 'GM', endings: [{ condition: 'maxTurns', tone: '终' }],
    localEvents: [
      { narrative: '需印记', summary: '魔事', requires: 'has(魔道)',
        choices: [{ text: 'a', effects: {} }, { text: 'b', effects: {} }] },
      { narrative: '通用', summary: '常事',
        choices: [{ text: 'a', effects: {} }, { text: 'b', effects: {} }] },
    ],
  })
  it('无印记时拿不到 has(魔道) 门控的事件', () => {
    const st = initState(sc, undefined, undefined, 'local')
    const seen = new Set<string>()
    for (let i = 0; i < 50; i++) seen.add(pickLocalEvent(sc, st, () => Math.random())!.summary)
    expect(seen.has('魔事')).toBe(false)
  })
  it('持有印记后可拿到', () => {
    const st = { ...initState(sc, undefined, undefined, 'local'), flags: ['魔道'] }
    let got = false
    for (let i = 0; i < 50 && !got; i++) if (pickLocalEvent(sc, st, () => Math.random())!.summary === '魔事') got = true
    expect(got).toBe(true)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/local.test.ts -t "印记门控"`
Expected: FAIL（门控未读 flags，魔事被抽到）

- [ ] **Step 3: 实现**

`src/scenarios/schema.ts` 的 `localEventSchema` 加一行（在 `keyMoment` 附近）：

```ts
  // 野事件：意外/奇遇，任何回合可低概率乱入（pickLocalEvent 给保底权重）
  wildcard: z.boolean().optional(),
```

`src/engine/local.ts` 的 `pickLocalEvent`，把 `meetsGate` 内的 `evalCondition` 调用补 flags 实参：

```ts
      if (!evalCondition(parseCondition(e.requires), st.attributes, turn, sc.maxTurns, st.flags ?? [])) return false
```

把 `progress` 计算改为兜底名义长度：

```ts
  const span = sc.maxTurns ?? 60
  const progress = span > 0 ? Math.min(1, turn / span) : 0.5
```

在 `effWeight` 上为 wildcard 加保底权重——把：

```ts
  const effWeight = (e: LocalEvent) =>
    (e.weight ?? 1) * (e.requiresItem ? 5 : e.requires ? 2 : 1) * stageBias(e)
```
改为：
```ts
  const effWeight = (e: LocalEvent) =>
    (e.weight ?? 1) * (e.requiresItem ? 5 : e.requires ? 2 : 1) * (e.wildcard ? 1.5 : 1) * stageBias(e)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/local.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/scenarios/schema.ts src/engine/local.ts src/engine/local.test.ts
git commit -m "feat(engine): 本地事件 has() 印记门控 + wildcard 保底 + progress 兜底"
```

---

## Task 9: localTurn 透传 outcomes/flags/endTone

**Files:**
- Modify: `src/engine/local.ts`（`localTurn`）
- Test: `src/engine/local.test.ts`

**Interfaces:**
- Produces: `localTurn` 把事件选项的 `outcomes/flagsSet/flagsClear/endTone` 带入 `TurnResult.choices`，使本地模式可触发分支/印记/强制结局。

- [ ] **Step 1: 写失败测试**

```ts
import { localTurn } from './local'

describe('localTurn 透传新字段', () => {
  const sc = scenarioSchema.parse({
    id: 'p', title: 'P', emoji: '🎴', intro: '开局',
    attributes: [{ key: 'hp', name: '生命', initial: 50, max: 100, deathBelow: 0 }],
    maxTurns: 30, systemPrompt: 'GM', endings: [{ condition: 'maxTurns', tone: '终' }],
    localEvents: [{
      narrative: '机缘', summary: '奇遇',
      choices: [
        { text: '赌', effects: {}, outcomes: [{ weight: 1, effects: { hp: 5 }, flagsSet: ['吉'] }] },
        { text: '走凶地', effects: {}, endTone: '暴毙' },
      ],
    }],
  })
  it('TurnResult 的 choices 携带 outcomes 与 endTone', () => {
    const st = initState(sc, undefined, undefined, 'local')
    const tr = localTurn(sc, st, () => 0)
    expect(tr.choices[0].outcomes?.[0].flagsSet).toEqual(['吉'])
    expect(tr.choices[1].endTone).toBe('暴毙')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/engine/local.test.ts -t "透传"`
Expected: FAIL（choices 丢了新字段）

- [ ] **Step 3: 实现**

`src/engine/local.ts` 的 `localTurn`，把 `choices` 映射从：

```ts
    choices: e.choices.map((c) => ({ text: c.text, effects: c.effects, reaction: c.reaction })),
```
改为：
```ts
    choices: e.choices.map((c) => ({
      text: c.text,
      effects: c.effects,
      reaction: c.reaction,
      ...(c.outcomes ? { outcomes: c.outcomes } : {}),
      ...(c.flagsSet ? { flagsSet: c.flagsSet } : {}),
      ...(c.flagsClear ? { flagsClear: c.flagsClear } : {}),
      ...(c.endTone ? { endTone: c.endTone } : {}),
    })),
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run src/engine/local.test.ts -t "透传"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/local.ts src/engine/local.test.ts
git commit -m "feat(engine): localTurn 透传 outcomes/印记/endTone"
```

---

## Task 10: 删除 Setup「人生长度」选项

**Files:**
- Modify: `src/ui/Setup.tsx`

**Interfaces:**
- Consumes: 无。
- Produces: Setup 不再缩放 `maxTurns`；`onStart` 直接传 `scenario`。

- [ ] **Step 1: 删状态**

删 `src/ui/Setup.tsx:38`：
```ts
  const [lenMult, setLenMult] = useState(1) // 人生长度倍率(基于剧本基准 maxTurns)
```

- [ ] **Step 2: 删面板**

删整段「人生长度」面板（`src/ui/Setup.tsx:283-304`，即 `<section className="panel"><h3 ...>人生长度</h3> ... </section>`）。

- [ ] **Step 3: 改 onStart**

把 start 按钮的 `onClick`（`src/ui/Setup.tsx:309-315`）从：
```ts
        onClick={() => {
          if (mode === 'ai') saveConfig(config())
          const sc =
            lenMult === 1
              ? scenario
              : { ...scenario, maxTurns: Math.round(scenario.maxTurns * lenMult) }
          onStart(sc, finalOpening(), mode === 'ai' ? ambition : '', mode)
        }}
```
改为：
```ts
        onClick={() => {
          if (mode === 'ai') saveConfig(config())
          onStart(scenario, finalOpening(), mode === 'ai' ? ambition : '', mode)
        }}
```

- [ ] **Step 4: 验证（tsc + build + 回归）**

Run: `npx tsc --noEmit && npx vitest run`
Expected: tsc 0；全部测试 PASS（无 `lenMult` 未用报错、无 `scenario.maxTurns` 取用 undefined 的崩溃）

- [ ] **Step 5: Commit**

```bash
git add src/ui/Setup.tsx
git commit -m "feat(ui): 删除 Setup 人生长度选项（长度改涌现/隐藏软上限）"
```

---

## Task 11: 全量回归 + 守护测试

**Files:**
- Modify:（按需）`src/scenarios/invariants.test.ts` 或新增 `src/scenarios/content-integrity.test.ts` 用例
- Test: 全量

**Interfaces:**
- Consumes: 全部前序任务。

- [ ] **Step 1: 写守护测试**

在 `src/scenarios/invariants.test.ts` 末尾加（确保新字段不破坏既有剧本加载，且所有结局条件可解析，含未来的 has()）：

```ts
import { builtinScenarios } from './index'
import { parseCondition } from '../engine/condition'

it('所有内置剧本的结局条件可解析（含 has()）', () => {
  for (const sc of builtinScenarios)
    for (const e of sc.endings) expect(() => parseCondition(e.condition)).not.toThrow()
})
```

- [ ] **Step 2: 跑全量**

Run: `npx tsc --noEmit && npx vitest run`
Expected: tsc 0；全部 PASS

- [ ] **Step 3: Commit**

```bash
git add src/scenarios/invariants.test.ts
git commit -m "test(engine): L1 守护测试（结局条件解析含 has）"
```

---

## Self-Review 结果

- **Spec 覆盖**：印记(§3)=T1/T2/T5；机缘封顶(§4)=T3；outcomes/因果(§5)=T4/T5；forceEnding/暴毙(§6)=T6；身份印记(§8)=T1；条件 has()=T2；maxTurns 可选+涌现长度(§9)=T7/T10；事件频率 wildcard(§5)=T8；本地透传=T9。L2 内容（xian 事件/世界框架/AI systemPrompt 与 AI 回合 schema 产出新字段）**不在本计划**，留待 L2 spec/plan。
- **类型一致性**：`evalCondition(c,attrs,turns,maxTurns?,flags?)`、`clampEffects(sc,attrs,effects,flags?)`、`checkEnding(sc,attrs,turns,flags?)`、`rollOutcome(choice,rng)`、`applyFlags(flags,set?,clear?)`、`effectiveCeiling(a,flags)`、`Outcome`/`Choice` 字段在各任务间一致。
- **占位符**：无 TODO/TBD；每个代码步骤含完整代码。
- **跨任务顺序依赖**：T5 调用的 `checkEnding(...,flags)` 在 T7 正式接收第 4 参；T5 测试用例刻意不触发 `checkEnding` 新行为，T7 Step 4 跑 tsc 收口。执行须按序。
